/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['unsplash.com', 'images.unsplash.com', 'plus.unsplash.com', 'media.discordapp.net', 'picsum.photos'],
    },
};

export default nextConfig;
